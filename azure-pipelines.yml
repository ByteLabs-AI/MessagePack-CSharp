# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

variables:
  - group: GlobalVariablesLibrary

pr:
  none

stages:
  - stage: ubuntu_latest
    displayName: 'ubuntu-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'ubuntu-latest'
    jobs:
      - job: Test
        displayName: '🚦 Test 🧩'
        dependsOn: [  ]
        strategy:
          parallel: 2
        steps:
          - task: NuGetAuthenticate@1
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: 'Cache: nuke-temp'
            inputs:
              key: $(Agent.OS) | nuke-temp | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: 'Cache: nuget-packages'
            inputs:
              key: $(Agent.OS) | nuget-packages | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            displayName: 'Run: Restore, Compile, Test'
            inputs:
              script: './build.cmd Restore Compile Test --skip --partition $(System.JobPositionInPhase)/2'
            env:
              SYSTEM_ACCESSTOKEN: $(System.AccessToken)
          - task: PublishBuildArtifacts@1
            displayName: 'Publish: test-results'
            inputs:
              artifactName: test-results
              pathToPublish: 'buildartifacts/test-results'
      - job: Publish
        displayName: '🚚 Publish'
        dependsOn: [ Test ]
        steps:
          - task: NuGetAuthenticate@1
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: 'Cache: nuke-temp'
            inputs:
              key: $(Agent.OS) | nuke-temp | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: 'Cache: nuget-packages'
            inputs:
              key: $(Agent.OS) | nuget-packages | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            displayName: 'Run: Restore, Compile, Pack, Publish'
            inputs:
              script: './build.cmd Restore Compile Pack Publish --skip'
            env:
              SYSTEM_ACCESSTOKEN: $(System.AccessToken)
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Test
        displayName: '🚦 Test 🧩'
        dependsOn: [  ]
        strategy:
          parallel: 2
        steps:
          - task: NuGetAuthenticate@1
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: 'Cache: nuke-temp'
            inputs:
              key: $(Agent.OS) | nuke-temp | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: 'Cache: nuget-packages'
            inputs:
              key: $(Agent.OS) | nuget-packages | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(USERPROFILE)/.nuget/packages
          - task: CmdLine@2
            displayName: 'Run: Restore, Compile, Test'
            inputs:
              script: './build.cmd Restore Compile Test --skip --partition $(System.JobPositionInPhase)/2'
            env:
              SYSTEM_ACCESSTOKEN: $(System.AccessToken)
          - task: PublishBuildArtifacts@1
            displayName: 'Publish: test-results'
            inputs:
              artifactName: test-results
              pathToPublish: 'buildartifacts/test-results'
      - job: Publish
        displayName: '🚚 Publish'
        dependsOn: [ Test ]
        steps:
          - task: NuGetAuthenticate@1
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: 'Cache: nuke-temp'
            inputs:
              key: $(Agent.OS) | nuke-temp | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: 'Cache: nuget-packages'
            inputs:
              key: $(Agent.OS) | nuget-packages | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(USERPROFILE)/.nuget/packages
          - task: CmdLine@2
            displayName: 'Run: Restore, Compile, Pack, Publish'
            inputs:
              script: './build.cmd Restore Compile Pack Publish --skip'
            env:
              SYSTEM_ACCESSTOKEN: $(System.AccessToken)
  - stage: macOS_latest
    displayName: 'macOS-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'macOS-latest'
    jobs:
      - job: Test
        displayName: '🚦 Test 🧩'
        dependsOn: [  ]
        strategy:
          parallel: 2
        steps:
          - task: NuGetAuthenticate@1
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: 'Cache: nuke-temp'
            inputs:
              key: $(Agent.OS) | nuke-temp | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: 'Cache: nuget-packages'
            inputs:
              key: $(Agent.OS) | nuget-packages | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            displayName: 'Run: Restore, Compile, Test'
            inputs:
              script: './build.cmd Restore Compile Test --skip --partition $(System.JobPositionInPhase)/2'
            env:
              SYSTEM_ACCESSTOKEN: $(System.AccessToken)
          - task: PublishBuildArtifacts@1
            displayName: 'Publish: test-results'
            inputs:
              artifactName: test-results
              pathToPublish: 'buildartifacts/test-results'
      - job: Publish
        displayName: '🚚 Publish'
        dependsOn: [ Test ]
        steps:
          - task: NuGetAuthenticate@1
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: 'Cache: nuke-temp'
            inputs:
              key: $(Agent.OS) | nuke-temp | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: 'Cache: nuget-packages'
            inputs:
              key: $(Agent.OS) | nuget-packages | global.json, src/**/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            displayName: 'Run: Restore, Compile, Pack, Publish'
            inputs:
              script: './build.cmd Restore Compile Pack Publish --skip'
            env:
              SYSTEM_ACCESSTOKEN: $(System.AccessToken)
